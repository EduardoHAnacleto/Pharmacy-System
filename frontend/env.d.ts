/// <reference types="vite/client" />

// No VITE_* variables are read by this app on purpose. Vite inlines them at build
// time, which would bake shop-specific values into the image; everything
// configurable comes from /api/v1/store-settings at runtime instead.
